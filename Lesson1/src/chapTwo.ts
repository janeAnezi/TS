let isRightColor: boolean;
let day: string;
let acc: number;
let stat: any;

isRightColor = true;
day = "Monday";
acc = 0.5;
stat = "hello";

console.log(`${stat} ${day} ${acc} ${isRightColor} of everything.`);
