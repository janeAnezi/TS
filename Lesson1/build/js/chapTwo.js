"use strict";
let isRightColor;
let day;
let acc;
let stat;
let album; // union type
let postId;
let isActive;
let regex = /\w+/g;
isRightColor = true;
day = "Monday";
acc = 0.5;
stat = "hello";
album = 123; // 'joey'
console.log(`${stat} ${day} ${acc} ${isRightColor} of everything.`);
let sum = (a, b) => {
    return a + b;
};
